{
  "buildCommand": "cd ../.. && pnpm install --no-frozen-lockfile && npx prisma generate && next build",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "ignoreCommand": "node -e \"process.exit(process.env.VERCEL_GIT_COMMIT_REF === 'main' ? 0 : 1)\""
}
